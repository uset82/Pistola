# Mapping Checklist

- Can the request reuse an existing node type?
- Which package owns the change?
- Which store actions are affected?
- Which system recomputes geometry or transforms?
- Which renderer or editor tool must change?
- Which existing files are the closest patterns?
- What command should validate the affected area?
- What architecture rule could this change violate?
