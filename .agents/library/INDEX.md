# Pistola object library

Approved, reusable builds. Each row points to `objects/<slug>.json`, which holds a blueprint and replayable action batches. Maintained with `$pistola-learnings`; read it at the start of `$pistola-studio`.

| Slug | Title | Keywords | Approved | Notes |
|---|---|---|---|---|

The table is empty until the first approved build. The boat in `.agents/skills/pistola-image-to-blueprint/references/sample-blueprint.json` is a format example, not an approved build.
