/** Default Three.js layer for main scene geometry. */
export const SCENE_LAYER = 0

/** Editor helper layer (grid, cursors, snap guides). */
export const EDITOR_LAYER = 1

/** Layer used for zone rendering (floor fills and wall borders). */
export const ZONE_LAYER = 2

/** Shadow-only receivers (ground disc / horizon) from the public lighting pass. */
export const SHADOW_ONLY_LAYER = 3
